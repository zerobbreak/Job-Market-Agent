# --- Model Provider (required) ---
# Get a key from https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-REPLACE_ME

# Default model key (optional). See main.py for supported keys.
OPENROUTER_MODEL_KEY=deepseek-chat

# --- Server / CORS ---
ALLOWED_ORIGINS=http://localhost:3000

# --- Database ---
# Use SQLite in dev (implicit). For production, set Postgres URL.
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# --- Repository Automation ---
# HTTPS URL for the repository the team will operate on.
CURRENT_REPO_URL=https://github.com/owner/repo

# Token with minimal repo permissions (read/write, create PR). Do NOT commit real tokens.
GIT_TOKEN=ghp_REPLACE_ME

# Allowed repo hosts (comma-separated)
REPO_ALLOWLIST=github.com,gitlab.com

# Branch prefix for automated branches
BRANCH_PREFIX=auto

# Commit identity for automated changes
GIT_USERNAME=agno-bot
GIT_EMAIL=agno-bot@example.com

# --- Team Configuration ---
# Choose team configuration: default, security_focused, minimal
TEAM_CONFIG=default

# --- Misc ---
# Disable telemetry if desired
# AGNO_TELEMETRY=false

# Optional API key to protect /tasks endpoint (send as X-API-Key header)
APP_API_KEY=change-me

